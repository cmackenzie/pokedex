import { Paper } from '@mui/material';
import { spacing } from '@mui/system';
import styled from '@emotion/styled';

const PaperSpacing = styled(Paper)(spacing);

export const PokePaper = styled(PaperSpacing)`
  background-color: #eee;
`;

